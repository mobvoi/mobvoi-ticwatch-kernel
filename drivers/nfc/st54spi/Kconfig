config NFC_ST54SPI
	tristate "STMicroelectronics ST54SPI NFC driver"
    ---help---
	  STMicroelectronics ST21NFCD core driver. It implements the chipset
	  NCI logic and hooks into the NFC kernel APIs. Physical layers will
	  register against it.

	  Say N if unsure.
